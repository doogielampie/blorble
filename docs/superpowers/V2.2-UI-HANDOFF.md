# v2.2 UI Handoff — board-first + curtain + practice flow (execution session)

**Mission:** Implement the LOCKED v2.2 UI design: remove the landing screen, add the
board-first top bar + curtain, the P2c practice flow, the F6/P3 results dialogs, and the
legend fix. Design is FINAL — do not redesign; build what the locked mockups show.

## Canonical inputs (in this repo)

- **Spec (governs behaviour):** `docs/superpowers/specs/2026-07-03-blorble-v2.2-ui-design.md`
- **Locked mockups (govern visuals):** `design/blorble-v2.2-ui/mockups.html` — key rows:
  R2 lock sheet L1-L5 (`r2.png`: fresh/playing/done/results/375-fit) and R4 practice flow
  P2c + P3 (`r4.png`). Rows for rejected variants (F2/F3/G2/G3/F7/P2a/P2b/P2d) remain in the
  file as history — do NOT build those.
- Prior-session ledger: `.superpowers/sdd/progress.md` (git-ignored, local).

## Process (the protocol that shipped v1, v2, v2.1, art-v2)

1. superpowers:writing-plans → full TDD implementation plan from the spec (bite-sized tasks,
   complete code, exact test pins).
2. superpowers:subagent-driven-development to execute: fresh implementer per task, task
   review each, final whole-branch review (most capable model) before push.
3. `pnpm check` green at every commit. Deploy = push to main (GitHub Pages via Actions;
   NO `gh` CLI on this machine — verify deploys by polling the live bundle).

## Hard constraints

- `renderBlorb(card, uid, expression)` and all art untouched (art v2 locked 2026-07-03).
- `src/seed.ts`, `src/board.ts`, `src/share.ts` byte-identical. `src/state.ts`: key
  `blorble.v1` and all existing v:2 fields unchanged; last-played-mode may be ADDED as an
  optional field — old saves must load unchanged (test this).
- Share text format unchanged. One layout everywhere (no desktop side rail).
- Dev/raster params keep semantics: `?autoplay=1` skips the curtain; `?practice=1`,
  `?mode=`, `?date=`, `?solve=`, `?hint=` unchanged (raster flows depend on them).
- Board cap: `46dvh` → ~`43dvh` (stacked bar is ~40px taller) — verify one-screen fit.

## Toolchain gotchas (paid for — don't rediscover)

- Phone rasters MUST use `tools/phone-shot.sh OUT.png W H "/blorble/?query" [PROFILE] [PORT]`
  (headless Chrome clamps width ≥500px; PNG is (W+200)×(H+200), judge top-left W×H).
- Board/result shots need `&autoplay=1` / `&autoplay=1&solve=N` (there is no landing anymore
  once this ships — but the curtain replaces it as the thing autoplay skips).
- Static grids: `--headless=new --screenshot --window-size=... file://…` with
  `--user-data-dir=$(mktemp -d)`, backgrounded, poll for file, `pkill -f headless=new`.
- Global `pnpm` (no corepack). Vitest on Node 25 sometimes SIGABRTs after green — rerun once.
- Never screenshot the Vite dev server.

## Definition of done

1. `pnpm check` green (incl. updated pinned tests for removed landing / new bar states).
2. Raster verification at 390×844 AND 375×667: fresh curtain, mid-game (centered timer),
   done state (`n/n ✓ · Results · practice`), practice (third segment) , daily results (F6),
   practice results (P3 New board), how-to legend spacing — judged against the locked
   mockups, with USER look approval on the final pass.
3. Existing-save resume verified (schema addition non-breaking); date-rollover behaviour
   (curtain returns, ✓s reset) exercised via `?date=`.
4. Deployed; live smoke; ledger + project memory updated.
